//
//  dbCode.swift
//  umc_forDB
//
//  Created by 김아림 on 2022/11/07.
//

import Foundation


/*
 
 // 회원가입
 아이디 값과 비밀번호 값을 가져오고 저장
 
 id가 key(두번째인자) 값password는 (첫번째 인자)
 UserDefaults.standard.set(password, id)
 // -> "아이디" : "비밀번호"
 
 print("이름:\(id) , 비밀번호:\(password)")
 navigationController?.popViewController(true) <- 자동으로 나가지도록 함
 
 
 
 // 로그인
 guard let name = nameTextField.text else {return} <- 이런식으로 nil방지
 
 if UserDefaults.standard.string(name) == nil {
    print("no data")
 }else if UserDefaults.standard.string(name) != password {
    print("error password")
 }else{
    print("success")
 }
 
 
 */
